version: 3
projects:
- name: my-project
  dir: .
  autoplan:
    enabled: true
  workflow: gcs-workflow
workflows:
  gcs-workflow:
    plan:
      steps:
      - run: terraform init -reconfigure # 💡 关键：强制重新配置 Backend
      - plan
    apply:
      steps:
      - apply
